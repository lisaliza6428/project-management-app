export interface BoardsModel {
  id: string,
  title: string,
  description: string,
}

export interface DialogData {
  title: string;
  description: string;
}

export interface ListModel {
  id: string,
  title: string,
  order: number,
}
